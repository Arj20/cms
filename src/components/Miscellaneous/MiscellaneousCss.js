import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  ImageMasonaryRoot: {
    width: 500,
    height: 450,
    margin: "2rem auto",
  },
  Loader: {
    display: "flex",
    justifyContent: "center",
  },
  SearchBox: {
    width: "100%",
    marginTop: "1rem",
    fontSize: "1.4rem",
  },
});
